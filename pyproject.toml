[tool.poetry]
name = "pysnyk"
version = "0.1.0"
description = "A Python client for the Snyk API"
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.22"
xlsxwriter = {version = "^1.1",optional = true}
mashumaro = "^1.7"

[tool.poetry.dev-dependencies]
pytest = "^4.6"
pytest-black = "^0.3.7"
pytest-cov = "^2.7"
pytest-mypy = "^0.3.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
